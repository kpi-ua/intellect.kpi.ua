import React from 'react';
import SectionTitle from '../common/SectionTitle';

const IProfileDetails: React.FC = () => {
    return (
        <div>
            <SectionTitle className="uppercase text-primary">Загальна інформація</SectionTitle>
        </div>
    );
};

export default IProfileDetails;
